# Plugins

This is the built-in plugin directory.

Use this folder to store preset plugins that can be loaded by the project at runtime.

Recommended structure:

- One plugin per subdirectory
- Keep each plugin self-contained
- Expose a clear entry file for each plugin

Example:

```text
plugins/
  example-plugin/
    index.ts
```
